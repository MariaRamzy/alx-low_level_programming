#include <stdio.h>
#include "main.h"

/**
 * _isdigit - checks for characters that are digits
 * @a: character
 * @b: character
 *
 * Return: multiplication result
 */

int mul(int a, int b)
{
return (a * b);
}
