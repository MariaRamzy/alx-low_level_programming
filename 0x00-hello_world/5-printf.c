#include <stdio.h>

/**
 * main -  prints exact with proper grammar, but the outcome is a piece of art,
 *
 * Return: Always return 0 (Success) program stopped
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
